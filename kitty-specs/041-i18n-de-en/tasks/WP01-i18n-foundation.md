---
work_package_id: WP01
title: i18n Foundation (Bootstrap, Persistence, API)
dependencies: []
requirement_refs:
- FR-001
- FR-004
- FR-005
- FR-006
planning_base_branch: main
merge_target_branch: main
branch_strategy: Planning artifacts for this feature were generated on main. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into main unless the human explicitly redirects the landing branch.
base_branch: main
base_commit: 0b24329afc0c853e39cfc6c37ce7242782834eea
created_at: '2026-04-19T12:10:35.693066+00:00'
subtasks: [T001, T002, T003, T004, T005, T006]
shell_pid: "17248"
history:
- at: '2026-04-19T09:27:55Z'
  by: claude
  note: WP generated by /spec-kitty.tasks
authoritative_surface: js/i18n.js
execution_mode: code_change
lane: planned
owned_files:
- js/i18n.js
- js/persistence.js
- index.html
---

# WP01 — i18n Foundation

## Branch Strategy

- Planning/base branch: `main`
- Merge target: `main`
- This WP is upstream root for WP02–WP08; they will stack on it via `--base WP01`.

## Objective

Lege das i18n-Plumbing: `window.i18n` API, Persistence-Erweiterung, Bootstrap-Reihenfolge in `index.html`. Keine UI-Migration in diesem WP — nur die Infrastruktur, auf der WP02–WP08 aufbauen.

## Context

- **Spec**: `kitty-specs/041-i18n-de-en/spec.md`
- **Plan**: `kitty-specs/041-i18n-de-en/plan.md`
- **Contract**: `kitty-specs/041-i18n-de-en/contracts/i18n-api.md` (das ist die Source-of-Truth für die API-Signaturen)
- **Codebase facts**:
  - Projekt nutzt klassische `<script>`-Tags (kein ESM, kein Build).
  - `js/persistence.js` hat bereits ein `KEYS.SETTINGS = 'demonfall_settings_v1'` Registry mit `loadSettings()`/`saveSettings()`.
  - `let`-Top-Level-Bindings sind script-scoped, NICHT auf `window` (siehe Memory `project_classic_script_scope.md`). i18n wird daher konsequent über `window.i18n` exposed.

## Implementation

### Subtask T001 — `js/i18n.js` mit API-Skeleton

**Purpose**: Erstelle das i18n-Modul gemäß `contracts/i18n-api.md`.

**Steps**:
1. Datei `js/i18n.js` neu anlegen.
2. Klassisches IIFE-Modul, das `window.i18n` setzt:
   ```js
   (function () {
     const dicts = { de: {}, en: {} };
     let active = 'de';
     const subscribers = new Set();

     function register(lang, dict) { /* ... */ }
     function t(key, params) { /* ... */ }
     function setLanguage(lang) { /* ... */ }
     function getLanguage() { return active; }
     function onChange(cb) { subscribers.add(cb); return () => subscribers.delete(cb); }

     window.i18n = { register, t, setLanguage, getLanguage, onChange };
   })();
   ```
3. `register(lang, dict)`:
   - Validiert `lang ∈ {'de','en'}` (sonst `console.warn` und Return)
   - `Object.assign(dicts[lang], dict)` — shallow merge, idempotent.
4. Keine externen Dependencies, kein localStorage-Zugriff (das macht persistence.js).

**Files**: `js/i18n.js` (NEU, ~60 Zeilen)

**Validation**: In Browser-Konsole nach Reload `typeof window.i18n.register === 'function'` → `true`.

---

### Subtask T002 — `js/persistence.js` Language-Wrapper

**Purpose**: Sprache als Feld in bestehender Settings-Persistenz.

**Steps**:
1. `js/persistence.js` lesen, `loadSettings()`/`saveSettings()` finden.
2. Zwei neue Funktionen anhängen + auf `window.persistence` exposen:
   ```js
   function getLanguage() {
     const s = loadSettings() || {};
     const lang = s.language;
     return (lang === 'de' || lang === 'en') ? lang : 'de';
   }
   function setLanguage(lang) {
     if (lang !== 'de' && lang !== 'en') {
       console.warn('[persistence] invalid language:', lang);
       return;
     }
     const s = loadSettings() || {};
     s.language = lang;
     saveSettings(s);
   }
   ```
3. Existierende `window.persistence`-Exporte um `getLanguage`, `setLanguage` ergänzen.

**Files**: `js/persistence.js` (modify)

**Validation**:
- Leerer localStorage → `persistence.getLanguage() === 'de'`
- `persistence.setLanguage('en')` → in DevTools Application-Tab: `demonfall_settings_v1.language === 'en'`
- `persistence.setLanguage('fr')` → Warning, kein Schreibvorgang

---

### Subtask T003 — `index.html` Bootstrap-Reihenfolge

**Purpose**: Garantiere, dass `i18n.t()` beim ersten Verbraucher-Aufruf funktional ist und die persistierte Sprache aktiv ist BEVOR Phaser bootet.

**Steps**:
1. `index.html` öffnen, Script-Tags lokalisieren.
2. `js/persistence.js` muss VOR `js/i18n.js` stehen (i18n nutzt persistence beim Bootstrap nicht direkt — aber Inline-Snippet braucht beides).
3. Direkt nach `<script src="js/i18n.js"></script>` ein Inline-Bootstrap einfügen:
   ```html
   <script src="js/persistence.js"></script>
   <script src="js/i18n.js"></script>
   <script>window.i18n.setLanguage(window.persistence.getLanguage());</script>
   <!-- alle weiteren Module + Phaser -->
   ```
4. Sicherstellen, dass diese Block VOR `js/main.js`, `js/inventory.js`, `js/eventSystem.js`, allen Scenes etc. steht.

**Files**: `index.html` (modify)

**Validation**: In Konsole _vor_ jedem anderen `console.log`: `console.log('lang at boot:', window.i18n.getLanguage())` → `'de'` bei leerem Storage, persistiert sonst.

---

### Subtask T004 — `setLanguage` Validation

**Purpose**: Robust gegen Garbage-Input.

**Steps**:
1. In `js/i18n.js` `setLanguage(lang)`:
   ```js
   function setLanguage(lang) {
     if (lang !== 'de' && lang !== 'en') {
       console.warn('[i18n] invalid language, falling back to de:', lang);
       lang = 'de';
     }
     if (lang === active) return;
     active = lang;
     subscribers.forEach((cb) => { try { cb(lang); } catch (e) { console.error(e); } });
   }
   ```
2. Subscriber-Aufrufe in try/catch — ein bösartiger Subscriber soll andere nicht abreißen.

**Files**: `js/i18n.js` (modify)

**Validation**:
- `i18n.setLanguage('xx')` → Console-Warning, `i18n.getLanguage() === 'de'`.
- `i18n.setLanguage('de')` während aktiv `'de'` → kein Subscriber-Call (weil `active === lang` Early-Return).

---

### Subtask T005 — `t()` Fallback-Kaskade

**Purpose**: Robuste Lookup-Logik mit klarer Diagnose bei Lücken.

**Steps**:
1. `t(key, params)`:
   ```js
   function t(key, params) {
     let val = dicts[active] && dicts[active][key];
     if (val === undefined) val = dicts.de && dicts.de[key];
     if (val === undefined) {
       console.warn('[i18n] missing key:', key);
       return '[MISSING:' + key + ']';
     }
     if (params) {
       Object.keys(params).forEach((k) => {
         val = val.split('{' + k + '}').join(String(params[k]));
       });
     }
     return val;
   }
   ```
2. Interpolation: `{name}` wird durch `params.name` ersetzt. Kein Escaping (Phaser-Texte sind plain).
3. Console-Warning ist absichtlich noisy — wir wollen Lücken in Playtests sofort sehen.

**Files**: `js/i18n.js` (modify)

**Validation**:
- `i18n.register('de', { greet: 'Hallo {name}' })` → `i18n.t('greet', {name: 'Welt'})` → `'Hallo Welt'`
- `i18n.t('missing.key')` → `'[MISSING:missing.key]'` + Warning
- Mit aktiver `'en'` und Key nur in `'de'` registriert → DE-Wert (Fallback funktioniert)

---

### Subtask T006 — Manuelle Browser-Verifikation

**Purpose**: Sanity-Check des kompletten Plumbings.

**Steps** (Browser-Konsole, `http://localhost:3000`):
1. `localStorage.clear()` + Reload
2. `window.i18n.getLanguage()` → `'de'`
3. `window.persistence.getLanguage()` → `'de'`
4. `window.i18n.register('de', { 'foo': 'Hallo' })`
5. `window.i18n.register('en', { 'foo': 'Hello' })`
6. `window.i18n.t('foo')` → `'Hallo'`
7. `window.i18n.setLanguage('en'); window.persistence.setLanguage('en')`
8. `window.i18n.t('foo')` → `'Hello'`
9. Reload → `window.i18n.getLanguage()` → `'en'` (persistiert)
10. `window.i18n.t('xxx')` → `'[MISSING:xxx]'` + Warning sichtbar
11. `localStorage.clear()` + Reload (Cleanup für nächste WP)

**Files**: keine — nur Verifikation.

**Validation**: alle 11 Schritte liefern erwartete Ergebnisse, keine Console-Errors (nur die explizit erwartete Missing-Warning).

---

## Definition of Done

- [ ] `js/i18n.js` exposed `window.i18n` mit allen 5 Methoden gemäß `contracts/i18n-api.md`
- [ ] `js/persistence.js` exposed `window.persistence.getLanguage` + `setLanguage`
- [ ] `index.html` lädt Module in korrekter Reihenfolge, Bootstrap-Snippet setzt Sprache vor Phaser-Init
- [ ] Validation-Schritte aus T006 alle grün
- [ ] Keine Console-Errors, keine Spielregressionen (HUD, Combat, Settings öffnen sich noch normal)
- [ ] Auto-commit auf `main`

## Risks

- **Bootstrap-Reihenfolge falsch**: wenn Phaser-Module vor i18n laden, bekommen sie `undefined`-Lookups. → Mitigation: Inline-Snippet IMMER direkt nach i18n.js, vor allem anderen.
- **localStorage-Format-Konflikt**: `loadSettings()` könnte ein nicht-Object-Ergebnis liefern (wenn manuell korrupte Daten). → Mitigation: `s = loadSettings() || {}` Default-Pattern.

## Reviewer Guidance

- Prüfe `index.html` Reihenfolge: persistence → i18n → bootstrap-snippet → alles andere.
- Bestätige, dass `window.i18n` und `window.persistence` global verfügbar sind nach Reload.
- T006-Browserschritte einmal manuell durchspielen.

## Implementation Command

```bash
spec-kitty implement WP01
```
